# Приём ввода из командной строки в Node.js

Как сделать CLI-программу на Node.js интерактивной?

Node.js начиная с версии 7 предоставляет [модуль `readline`](https://nodejs.org/docs/latest-v22.x/api/readline.html), предназначенный именно для этого: получать ввод из читаемого потока, такого как поток `process.stdin`, который во время выполнения программы Node.js является терминальным вводом, по одной строке за раз.
```js
const readline = require('node:readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});

rl.question(`What's your name?`, name => {
  console.log(`Hi ${name}!`);
  rl.close();
});
```

Этот фрагмент кода запрашивает _имя_ пользователя, и как только текст введён и пользователь нажимает Enter, мы отправляем приветствие.

Метод `question()` показывает первый параметр (вопрос) и ждёт ввода пользователя. Он вызывает функцию-колбэк после нажатия Enter.

В этой функции-колбэке мы закрываем интерфейс readline.

`readline` предлагает несколько других методов, ознакомьтесь с ними в документации пакета, ссылка на которую приведена выше.

Если вам нужно запросить пароль, лучше не выводить его обратно на экран, а вместо этого показывать символ `*`.

---

> _Перевод официальной документации Node.js (раздел Learn). Источник: https://nodejs.org/en/learn/command-line/accept-input-from-the-command-line-in-nodejs. Оригинал распространяется по лицензии MIT._
