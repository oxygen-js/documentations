# every

#### сигнатура: `every(predicate: function, thisArg: any): Observable`

## Если все значения проходят предикат до завершения, испускает true, иначе false.



### Примеры

##### Пример 1: Некоторые значения false

(
[Stackblitz](https://stackblitz.com/edit/typescript-299d7s?file=index.ts&devtoolsheight=100)
| [jsBin](http://jsbin.com/cibijotase/1/edit?js,console) |
[jsFiddle](https://jsfiddle.net/btroncone/1b46tsm7/) )

```js
// RxJS v6+
import { every } from 'rxjs/operators';
import { of } from 'rxjs';

//испускаем 5 значений
const source = of(1, 2, 3, 4, 5);
const example = source.pipe(
  //является ли каждое значение чётным?
  every(val => val % 2 === 0)
);
//вывод: false
const subscribe = example.subscribe(val => console.log(val));
```

##### Пример 2: Все значения true

(
[Stackblitz](https://stackblitz.com/edit/typescript-ztrzqe?file=index.ts&devtoolsheight=100)
| [jsBin](http://jsbin.com/yuxefiviko/1/edit?js,console) |
[jsFiddle](https://jsfiddle.net/btroncone/x34nLmcj/) )

```js
// RxJS v6+
import { every } from 'rxjs/operators';
import { of } from 'rxjs';

//испускаем 5 значений
const allEvens = of(2, 4, 6, 8, 10);
const example = allEvens.pipe(
  //является ли каждое значение чётным?
  every(val => val % 2 === 0)
);
//вывод: true
const subscribe = example.subscribe(val => console.log(val));
```

##### Пример 3: Значения, приходящие с течением времени, и преждевременное завершение потока из-за того, что every возвращает false

(
[Stackblitz](https://stackblitz.com/edit/rxjs-every-example?file=index.ts&devtoolsheight=100)
)

```js
// RxJS v6+
console.clear();
import { concat, of } from 'rxjs';
import { every, delay, tap } from 'rxjs/operators';

const log = console.log;
const returnCode = request => (Number.isInteger(request) ? 200 : 400);
const fakeRequest = request =>
  of({ code: returnCode(request) }).pipe(
    tap(_ => log(request)),
    delay(1000)
  );

const apiCalls$ = concat(
  fakeRequest(1),
  fakeRequest('invalid payload'),
  fakeRequest(2) //это не выполнится, так как every вернёт false для предыдущей строки
).pipe(
  every(e => e.code === 200),
  tap(e => log(`all request successful: ${e}`))
);

apiCalls$.subscribe();
```

### Дополнительные материалы

- [every](https://rxjs.dev/api/operators/every) 📰 - Официальная документация

---

> 📁 Исходный код:
> [https://github.com/ReactiveX/rxjs/blob/master/packages/rxjs/src/internal/operators/every.ts](https://github.com/ReactiveX/rxjs/blob/master/packages/rxjs/src/internal/operators/every.ts)

---

> _Перевод документации Learn RxJS. Источник: https://www.learnrxjs.io/learn-rxjs/operators/conditional/every. Оригинал распространяется по лицензии MIT._
