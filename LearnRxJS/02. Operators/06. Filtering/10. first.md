# first

#### сигнатура: `first(predicate: function, select: function)`

## Испустить первое значение или первое, прошедшее заданное выражение.

---

💡 Противоположность first — это [**last**](<12. last.md>)!

💡 `First` доставит EmptyError в колбэк ошибки наблюдателя, если
Observable завершится до того, как будет отправлено хотя бы одно next-уведомление. Если вам не нужно
такое поведение, используйте вместо этого `take(1)`.

---



### Примеры

(
[example tests](https://github.com/btroncone/learn-rxjs/blob/master/operators/specs/filtering/first-spec.ts)
)

##### Пример 1: Первое значение из последовательности

(
[StackBlitz](https://stackblitz.com/edit/typescript-t8hseq?file=index.ts&devtoolsheight=100)
| [jsBin](http://jsbin.com/kayenuxoma/1/edit?js,console) |
[jsFiddle](https://jsfiddle.net/btroncone/uncey4v9/) )

```js
// RxJS v6+
import { from } from 'rxjs';
import { first } from 'rxjs/operators';

const source = from([1, 2, 3, 4, 5]);
//без аргументов, испускаем первое значение
const example = source.pipe(first());
//вывод: "First value: 1"
const subscribe = example.subscribe(val => console.log(`First value: ${val}`));
```

##### Пример 2: Первое значение, прошедшее предикат

(
[StackBlitz](https://stackblitz.com/edit/typescript-bw5byu?file=index.ts&devtoolsheight=100)
| [jsBin](http://jsbin.com/pujowawovu/1/edit?js,console) |
[jsFiddle](https://jsfiddle.net/btroncone/pt36r8cu/) )

```js
// RxJS v6+
import { from } from 'rxjs';
import { first } from 'rxjs/operators';

const source = from([1, 2, 3, 4, 5]);
//испускаем первый элемент, прошедший проверку
const example = source.pipe(first(num => num === 5));
//вывод: "First to pass test: 5"
const subscribe = example.subscribe(val =>
  console.log(`First to pass test: ${val}`)
);
```

##### Пример 3: Использование значения по умолчанию

(
[StackBlitz](https://stackblitz.com/edit/typescript-2pkzpv?file=index.ts&devtoolsheight=100)
| [jsBin](http://jsbin.com/qoganeleqa/1/edit?js,console) |
[jsFiddle](https://jsfiddle.net/btroncone/owx2jdg1/3/) )

```js
// RxJS v6+
import { from } from 'rxjs';
import { first } from 'rxjs/operators';

const source = from([1, 2, 3, 4, 5]);
//ни одно значение не пройдёт, испускаем значение по умолчанию
const example = source.pipe(first(val => val > 5, 'Nothing'));
//вывод: 'Nothing'
const subscribe = example.subscribe(val => console.log(val));
```

### Дополнительные материалы

- [first](https://rxjs.dev/api/operators/first) 📰 - Официальная документация
- [Filtering operator: take, first, skip](https://egghead.io/lessons/rxjs-filtering-operators-take-first-skip?course=rxjs-beyond-the-basics-operators-in-depth)
  🎥 💵 - André Staltz

---

> 📁 Исходный код:
> [https://github.com/ReactiveX/rxjs/blob/master/packages/rxjs/src/internal/operators/first.ts](https://github.com/ReactiveX/rxjs/blob/master/packages/rxjs/src/internal/operators/first.ts)

---

> _Перевод документации Learn RxJS. Источник: https://www.learnrxjs.io/learn-rxjs/operators/filtering/first. Оригинал распространяется по лицензии MIT._
