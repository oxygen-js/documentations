# Умный счётчик

Интересный элемент интерфейсов, где числа динамически обновляются, — это умный
счётчик, или эффект одометра. Вместо того чтобы просто перескакивать число вверх
или вниз, можно добиться классного эффекта, быстро отсчитывая до нужного числа.
Пример популярной библиотеки, которая это делает, —
[odometer](https://github.com/HubSpot/odometer) от
[Hubspot](https://github.com/HubSpot). Давайте посмотрим, как можно добиться
чего-то похожего всего несколькими строками RxJS.



#### Чистый JS

( [StackBlitz](https://stackblitz.com/edit/rxjs-m2zsud) )

![Умный счётчик](https://drive.google.com/uc?export=view&id=1cG8huWWfsqSe8jLed_NmLrENnF0QyhfZ)

```js
import { timer, fromEvent, merge } from 'rxjs';
import {
  switchMap,
  startWith,
  scan,
  takeWhile,
  takeUntil,
  filter,
  mapTo,
  map,
  tap,
  pluck
} from 'rxjs/operators';

let currentNumber = 0;

// элементы
const input: any = document.getElementById('range');

// вспомогательные функции
const takeUntilFunc = (endRange, currentNumber) => {
  return endRange > currentNumber
    ? val => val <= endRange
    : val => val >= endRange;
};
const positiveOrNegative = (endRange, currentNumber) => {
  return endRange > currentNumber ? 1 : -1;
};
const updateHTML = id => val => (document.getElementById(id).innerHTML = val);

// потоки
const enter$ = fromEvent(input, 'keyup').pipe(
  pluck('code'),
  filter(code => code === 'Enter')
);

enter$
  .pipe(
    map(() => parseInt(input.value)),
    switchMap(endRange => {
      return timer(0, 20).pipe(
        mapTo(positiveOrNegative(endRange, currentNumber)),
        startWith(currentNumber),
        scan((acc, curr) => acc + curr),
        takeWhile(takeUntilFunc(endRange, currentNumber))
      );
    }),
    tap(v => (currentNumber = v)),
    startWith(currentNumber)
  )
  .subscribe(updateHTML('display'));
```

###### HTML

```html
<div class="container">
  <input id="range" type="number" />
  <h1 id="display"></h1>
</div>
```

Мы легко можем взять наш умный счётчик на чистом JS и обернуть его в любую
популярную библиотеку компонентного UI. Ниже — пример компонента умного счётчика
на Angular, который принимает `Input` с обновлёнными конечными диапазонами и
выполняет соответствующий переход.

#### Версия на Angular

(
[StackBlitz](https://stackblitz.com/edit/angular-gcnqlq?file=app%2Fnumber-tracker.component.ts)
)

```js
import { Component, Input, OnDestroy } from '@angular/core';
import { Subject } from 'rxjs/Subject';
import { timer } from 'rxjs/observable/timer';
import { switchMap, startWith, scan, takeWhile, takeUntil, mapTo } from 'rxjs/operators';

@Component({
  selector: 'number-tracker',
  template: `
    <h3> {{ currentNumber }}</h3>
  `
})
export class NumberTrackerComponent implements OnDestroy {
  @Input()
  set end(endRange: number) {
    this._counterSub$.next(endRange);
  }
  @Input() countInterval = 20;
  public currentNumber = 0;
  private _counterSub$ = new Subject();
  private _onDestroy$ = new Subject();

  constructor() {
    this._counterSub$
      .pipe(
        switchMap(endRange => {
          return timer(0, this.countInterval).pipe(
            mapTo(this.positiveOrNegative(endRange, this.currentNumber)),
            startWith(this.currentNumber),
            scan((acc: number, curr: number) => acc + curr),
            takeWhile(this.isApproachingRange(endRange, this.currentNumber))
          )
        }),
        takeUntil(this._onDestroy$)
      )
      .subscribe((val: number) => this.currentNumber = val);
  }

  private positiveOrNegative(endRange, currentNumber) {
    return endRange > currentNumber ? 1 : -1;
  }

  private isApproachingRange(endRange, currentNumber) {
    return endRange > currentNumber
      ? val => val <= endRange
      : val => val >= endRange;
  }

  ngOnDestroy() {
    this._onDestroy$.next();
    this._onDestroy$.complete();
  }
}
```

###### HTML

```html
<p>
  <input
    type="number"
    (keyup.enter)="counterNumber = vanillaInput.value"
    #vanillaInput
  />
  <button (click)="counterNumber = vanillaInput.value">
    Update number
  </button>
</p>
<number-tracker [end]="counterNumber"></number-tracker>
```

### Используемые операторы

- [fromEvent](<../02. Operators/03. Creation/06. fromEvent.md>)
- [map](<../02. Operators/07. Transformation/11. map.md>)
- [mapTo](<../02. Operators/07. Transformation/12. mapTo.md>)
- [scan](<../02. Operators/07. Transformation/18. scan.md>)
- [startWith](<../02. Operators/01. Combination/11. startWith.md>)
- [switchMap](<../02. Operators/07. Transformation/19. switchMap.md>)
- [takeWhile](<../02. Operators/06. Filtering/21. takeWhile.md>)

---

> _Перевод документации Learn RxJS. Источник: https://www.learnrxjs.io/learn-rxjs/recipes/smartcounter. Оригинал распространяется по лицензии MIT._
