# HTTP-опрос



### Примеры

##### Пример 1

_Автор: [@barryrowe](https://twitter.com/barryrowe)_

Этот рецепт демонстрирует один из способов реализовать опрос HTTP-эндпоинта через
заданный интервал. Это распространённая задача в веб-приложениях, и RxJS обычно
справляется с ней очень хорошо, поскольку непрерывную последовательность
HTTP-запросов и ответов легко воспринимать как поток данных.

(
[StackBlitz](https://stackblitz.com/edit/rxjs-http-poll-recipe-jc5cj7?file=index.ts&devtoolsheight=50)
)

![HTTP-опрос](https://drive.google.com/uc?export=view&id=1HwHApLDoxO9Zc5DAG3XtgJBl83CpXmjU)

```js
// Импортируем стили
import './style.css';

import { Observable, Subscription, of, fromEvent, from, empty, merge, timer } from 'rxjs';
import { map, mapTo, switchMap, tap, mergeMap, takeUntil, filter, finalize } from 'rxjs/operators';

declare type RequestCategory = 'cats' | 'meats';

// Константы для запросов котиков
const CATS_URL = "https://placekitten.com/g/{w}/{h}";
function mapCats(response): Observable<string> {

  return from(new Promise((resolve, reject) => {
      var blob = new Blob([response], {type: "image/png"});
      let reader = new FileReader();
      reader.onload = (data: any) => {
        resolve(data.target.result);
      };
      reader.readAsDataURL(blob);
  }));
}

// Константы для запросов мяса
const MEATS_URL = "https://baconipsum.com/api/?type=meat-and-filler";
function mapMeats(response): Observable<string> {
  const parsedData = JSON.parse(response);
  return of(parsedData ? parsedData[0] : '');
}

/*************************
 * Наше рабочее состояние
 *************************/
 // Какой тип данных мы запрашиваем
let requestCategory: RequestCategory = 'cats';
// Текущая подписка на опрос
let pollingSub: Subscription;
/*************************/

/**
 * Эта функция выполнит AJAX-запрос к заданному Url, преобразует
 * распарсенный из JSON ответ с помощью переданной функции-маппера и испустит
 * результат в возвращаемый observable.
 */
function requestData(url: string, mapFunc: (any) => Observable<string>): Observable<string> {
  console.log(url)
  const xhr = new XMLHttpRequest();
  return from(new Promise<string>((resolve, reject) => {

    // Здесь генерируется случайный размер для изображения placekitten,
    //   чтобы при каждом запросе мы получали новых котиков.
    const w = Math.round(Math.random() * 400);
    const h = Math.round(Math.random() * 400);
    const targetUrl = url
      .replace('{w}', w.toString())
      .replace('{h}', h.toString());

    xhr.addEventListener("load", () => {
      resolve(xhr.response);
    });
    xhr.open("GET", targetUrl);
    if(requestCategory === 'cats') {
      // Наши url котиков возвращают бинарные данные,
      //  поэтому нам нужно указать соответствующий тип ответа.
      xhr.responseType = "arraybuffer";
    }
    xhr.send();
  }))
  .pipe(
    switchMap((data) => mapFunc(xhr.response)),
    tap((data) => console.log('Request result: ', data))
  );
}


/**
 * Эта функция начнёт наш опрос для заданного состояния
 * и с заданным интервалом (по умолчанию 5 секунд)
 */
function startPolling(category: RequestCategory, interval: number = 5000): Observable<string> {
  const url = category === 'cats' ? CATS_URL : MEATS_URL;
  const mapper = category === 'cats' ? mapCats : mapMeats;

  return timer(0, interval)
    .pipe(
      switchMap(_ => requestData(url, mapper))
    );
}

// Собираем наши DOM-элементы, чтобы привязать события
const startButton = document.getElementById('start');
const stopButton = document.getElementById('stop');
const text = document.getElementById('text');
const pollingStatus = document.getElementById('polling-status');
const catsRadio = document.getElementById('catsCheckbox');
const meatsRadio = document.getElementById('meatsCheckbox');
const catsClick$ = fromEvent(catsRadio, 'click').pipe(mapTo('cats'));
const meatsClick$ = fromEvent(meatsRadio, 'click').pipe(mapTo('meats'));
const catImage: HTMLImageElement = <HTMLImageElement>document.getElementById('cat');
// Останавливаем опрос
let stopPolling$ = fromEvent(stopButton, 'click');

function updateDom(result) {
  if (requestCategory === 'cats') {
    catImage.src = result;
    console.log(catImage);
  } else {
    text.innerHTML = result;
  }
}

function watchForData(category: RequestCategory) {
    // Запускаем новый опрос
    return startPolling(category, 5000).pipe(
      tap(updateDom),
      takeUntil(
        // прекращаем опрос при клике по кнопке или смене категории
        merge(
          stopPolling$,
          merge(catsClick$, meatsClick$).pipe(filter(c => c !== category))
        )
      ),
      // только для целей демонстрации
      finalize(() => pollingStatus.innerHTML = 'Stopped')
    )
}

// Обрабатываем обновления формы
catsClick$
  .subscribe((category: RequestCategory) => {
    requestCategory = category;
    catImage.style.display = 'block';
    text.style.display = 'none';
  });

meatsClick$
  .subscribe((category: RequestCategory) => {
    requestCategory = category;
    catImage.style.display = 'none';
    text.style.display = 'block';
  });

// Запускаем опрос
fromEvent(startButton, 'click')
.pipe(
  // только для целей демонстрации
  tap(_ => pollingStatus.innerHTML = 'Started'),
  mergeMap(_ => watchForData(requestCategory))
)
.subscribe();
```

### Используемые операторы

- [filter](<../02. Operators/06. Filtering/08. filter.md>)
- [fromEvent](<../02. Operators/03. Creation/06. fromEvent.md>)
- [from](<../02. Operators/03. Creation/05. from.md>)
- [map](<../02. Operators/07. Transformation/11. map.md>)
- [mapTo](<../02. Operators/07. Transformation/12. mapTo.md>)
- [merge](<../02. Operators/01. Combination/07. merge.md>)
- [mergeMap](<../02. Operators/07. Transformation/13. mergeMap - flatMap.md>)
- [switchMap](<../02. Operators/07. Transformation/19. switchMap.md>)
- [timer](<../02. Operators/03. Creation/12. timer.md>)

##### Пример 2: Простой HTTP-опрос

_Автор: [@adamlubek](https://github.com/adamlubek)_

Этот рецепт демонстрирует опрос HTTP-эндпоинта с помощью repeat. Он ждёт 3
секунды после ответа, прежде чем опросить снова. Код ниже упрощён, чтобы показать
самую суть решения, но по ссылке ниже приведён вариант с подробным логированием и
обработкой ошибок.

(
[StackBlitz](https://stackblitz.com/edit/rxjs-http-polling?file=index.ts&devtoolsheight=80)
)

```js
// RxJS v6+
import { of } from 'rxjs';
import { delay, tap, mergeMap, repeat } from 'rxjs/operators';

const fakeDelayedRequest = () => of(new Date()).pipe(delay(1000));

const display = response => {
  document.open();
  document.write(response);
};

const poll = of({}).pipe(
  mergeMap(_ => fakeDelayedRequest()),
  tap(display),
  delay(3000),
  repeat()
);

poll.subscribe();
```

---

> _Перевод документации Learn RxJS. Источник: https://www.learnrxjs.io/learn-rxjs/recipes/http-polling. Оригинал распространяется по лицензии MIT._
