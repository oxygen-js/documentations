# JavaScript API

JavaScript API Vite полностью типизирован, и рекомендуется использовать TypeScript или включить проверку типов JS в VS Code, чтобы задействовать автодополнение (intellisense) и валидацию.

## `createServer`

**Сигнатура типа:**

```ts
async function createServer(inlineConfig?: InlineConfig): Promise<ViteDevServer>
```

**Пример использования:**

```ts
import { createServer } from 'vite'

const server = await createServer({
  // любые допустимые опции пользовательской конфигурации, плюс `mode` и `configFile`
  configFile: false,
  root: import.meta.dirname,
  server: {
    port: 1337,
  },
})
await server.listen()

server.printUrls()
server.bindCLIShortcuts({ print: true })
```

> **Примечание**
>
> При использовании `createServer` и `build` в одном процессе Node.js обе функции для корректной работы полагаются на `process.env.NODE_ENV`, что также зависит от опции конфигурации `mode`. Чтобы избежать конфликтующего поведения, установите `process.env.NODE_ENV` или `mode` обоих API в значение `development`. В противном случае вы можете запустить дочерний процесс, чтобы выполнять эти API по отдельности.

> **Примечание**
>
> При использовании [режима middleware](https://vite.dev/config/server-options#server-middlewaremode) в сочетании с [настройкой проксирования для WebSocket](https://vite.dev/config/server-options#server-proxy) родительский http-сервер следует передать в `middlewareMode`, чтобы корректно привязать прокси.
>
> <details>
> <summary>Пример</summary>
>
> ```ts
> import http from 'http'
> import { createServer } from 'vite'
>
> const parentServer = http.createServer() // или express, koa и т. д.
>
> const vite = await createServer({
>   server: {
>     // Включаем режим middleware
>     middlewareMode: {
>       // Передаём родительский http-сервер для проксирования WebSocket
>       server: parentServer,
>     },
>     proxy: {
>       '/ws': {
>         target: 'ws://localhost:3000',
>         // Проксирование WebSocket
>         ws: true,
>       },
>     },
>   },
> })
>
> parentServer.use(vite.middlewares)
> ```
>
> </details>

## `InlineConfig`

Интерфейс `InlineConfig` расширяет `UserConfig` дополнительными свойствами:

- `configFile`: указывает файл конфигурации, который следует использовать. Если не задан, Vite попытается автоматически найти его в корне проекта. Установите `false`, чтобы отключить автоматический поиск.

## `ResolvedConfig`

Интерфейс `ResolvedConfig` содержит все те же свойства, что и `UserConfig`, за исключением того, что большинство свойств разрешены и не равны undefined. Он также содержит вспомогательные утилиты, такие как:

- `config.assetsInclude`: функция для проверки того, считается ли `id` ресурсом.
- `config.logger`: внутренний объект логгера Vite.

## `ViteDevServer`

```ts
interface ViteDevServer {
  /**
   * Разрешённый объект конфигурации Vite.
   */
  config: ResolvedConfig
  /**
   * Экземпляр connect-приложения.
   * - Можно использовать для подключения пользовательских middleware к dev-серверу.
   * - Также можно использовать как функцию-обработчик пользовательского http-сервера
   *   или как middleware в любых Node.js-фреймворках в стиле connect.
   *
   * https://github.com/senchalabs/connect#use-middleware
   */
  middlewares: Connect.Server
  /**
   * Нативный экземпляр http-сервера Node.
   * В режиме middleware будет null.
   */
  httpServer: http.Server | null
  /**
   * Экземпляр наблюдателя Chokidar. Если `config.server.watch` установлено в `null`,
   * он не будет отслеживать файлы, а вызовы `add` или `unwatch` не будут иметь эффекта.
   * https://github.com/paulmillr/chokidar/tree/3.6.0#api
   */
  watcher: FSWatcher
  /**
   * WebSocket-сервер с методом `send(payload)`.
   */
  ws: WebSocketServer
  /**
   * Контейнер плагинов Rollup, который может запускать хуки плагинов для заданного файла.
   */
  pluginContainer: PluginContainer
  /**
   * Граф модулей, отслеживающий отношения импорта, сопоставление url с файлами
   * и состояние hmr.
   */
  moduleGraph: ModuleGraph
  /**
   * Разрешённые url, которые Vite выводит в CLI (в URL-кодировке). Возвращает `null`
   * в режиме middleware или если сервер не слушает ни один порт.
   */
  resolvedUrls: ResolvedServerUrls | null
  /**
   * Программно разрешает, загружает и преобразует URL и возвращает результат
   * без прохождения через конвейер http-запросов.
   */
  transformRequest(
    url: string,
    options?: TransformOptions,
  ): Promise<TransformResult | null>
  /**
   * Применяет встроенные HTML-преобразования Vite и любые HTML-преобразования плагинов.
   */
  transformIndexHtml(
    url: string,
    html: string,
    originalUrl?: string,
  ): Promise<string>
  /**
   * Загружает заданный URL как инстанцированный модуль для SSR.
   */
  ssrLoadModule(
    url: string,
    options?: { fixStacktrace?: boolean },
  ): Promise<Record<string, any>>
  /**
   * Исправляет трассировку стека ошибки ssr.
   */
  ssrFixStacktrace(e: Error): void
  /**
   * Запускает HMR для модуля в графе модулей. Вы можете использовать API `server.moduleGraph`,
   * чтобы получить модуль, который нужно перезагрузить. Если `hmr` равно false, ничего не происходит.
   */
  reloadModule(module: ModuleNode): Promise<void>
  /**
   * Запускает сервер.
   */
  listen(port?: number, isRestart?: boolean): Promise<ViteDevServer>
  /**
   * Перезапускает сервер.
   *
   * @param forceOptimize - принудительно пересобрать зависимости оптимизатором, аналогично флагу --force в CLI
   */
  restart(forceOptimize?: boolean): Promise<void>
  /**
   * Останавливает сервер.
   */
  close(): Promise<void>
  /**
   * Привязывает горячие клавиши CLI
   */
  bindCLIShortcuts(options?: BindCLIShortcutsOptions<ViteDevServer>): void
  /**
   * Вызов `await server.waitForRequestsIdle(id)` будет ждать, пока не будут обработаны все
   * статические импорты. Если он вызывается из хука плагина load или transform, id нужно
   * передавать в качестве параметра во избежание взаимоблокировок. Вызов этой функции после того,
   * как первая секция статических импортов графа модулей уже обработана, разрешится немедленно.
   * @experimental
   */
  waitForRequestsIdle: (ignoredId?: string) => Promise<void>
}
```

> **Информация**
>
> `waitForRequestsIdle` предназначена для использования в качестве «запасного выхода» (escape hatch) для улучшения опыта разработки (DX) в случае возможностей, которые нельзя реализовать в рамках работающей «по требованию» природы dev-сервера Vite. Её можно использовать во время запуска такими инструментами, как Tailwind, чтобы отложить генерацию CSS-классов приложения до тех пор, пока не будет просмотрен код приложения, избегая мельканий при изменении стилей. Когда эта функция используется в хуке load или transform и применяется стандартный сервер HTTP1, один из шести http-каналов будет заблокирован, пока сервер не обработает все статические импорты. Оптимизатор зависимостей Vite в настоящее время использует эту функцию, чтобы избежать полной перезагрузки страницы при отсутствующих зависимостях, откладывая загрузку предварительно собранных зависимостей до тех пор, пока все импортированные зависимости не будут собраны из статически импортированных источников. В будущем мажорном релизе Vite может перейти на другую стратегию, установив `optimizeDeps.crawlUntilStaticImports: false` по умолчанию, чтобы избежать снижения производительности в крупных приложениях при холодном старте.

## `build`

**Сигнатура типа:**

```ts
async function build(
  inlineConfig?: InlineConfig,
): Promise<RolldownOutput | RolldownOutput[] | RolldownWatcher>
```

**Пример использования:**

**vite.config.js**

```ts
import path from 'node:path'
import { build } from 'vite'

await build({
  root: path.resolve(import.meta.dirname, './project'),
  base: '/foo/',
  build: {
    rolldownOptions: {
      // ...
    },
  },
})
```

## `preview`

**Сигнатура типа:**

```ts
async function preview(inlineConfig?: InlineConfig): Promise<PreviewServer>
```

**Пример использования:**

```ts
import { preview } from 'vite'

const previewServer = await preview({
  // любые допустимые опции пользовательской конфигурации, плюс `mode` и `configFile`
  preview: {
    port: 8080,
    open: true,
  },
})

previewServer.printUrls()
previewServer.bindCLIShortcuts({ print: true })
```

## `PreviewServer`

```ts
interface PreviewServer {
  /**
   * Разрешённый объект конфигурации vite
   */
  config: ResolvedConfig
  /**
   * Экземпляр connect-приложения.
   * - Можно использовать для подключения пользовательских middleware к серверу предпросмотра.
   * - Также можно использовать как функцию-обработчик пользовательского http-сервера
   *   или как middleware в любых Node.js-фреймворках в стиле connect
   *
   * https://github.com/senchalabs/connect#use-middleware
   */
  middlewares: Connect.Server
  /**
   * нативный экземпляр http-сервера Node
   */
  httpServer: http.Server
  /**
   * Разрешённые url, которые Vite выводит в CLI (в URL-кодировке). Возвращает `null`,
   * если сервер не слушает ни один порт.
   */
  resolvedUrls: ResolvedServerUrls | null
  /**
   * Выводит url сервера
   */
  printUrls(): void
  /**
   * Привязывает горячие клавиши CLI
   */
  bindCLIShortcuts(options?: BindCLIShortcutsOptions<PreviewServer>): void
}
```

## `resolveConfig`

**Сигнатура типа:**

```ts
async function resolveConfig(
  inlineConfig: InlineConfig,
  command: 'build' | 'serve',
  defaultMode = 'development',
  defaultNodeEnv = 'development',
  isPreview = false,
): Promise<ResolvedConfig>
```

Значение `command` равно `serve` в режимах разработки и предпросмотра и `build` при сборке.

## `mergeConfig`

**Сигнатура типа:**

```ts
function mergeConfig(
  defaults: Record<string, any>,
  overrides: Record<string, any>,
  isRoot = true,
): Record<string, any>
```

Выполняет глубокое слияние двух конфигураций Vite. `isRoot` обозначает уровень внутри конфигурации Vite, который подвергается слиянию. Например, установите `false`, если вы объединяете две группы опций `build`.

Обратите внимание, что значения `null` и `undefined` в `overrides` пропускаются и не участвуют в слиянии. Если вам нужно явно очистить значение из `defaults`, измените результат `mergeConfig` напрямую.

> **Примечание**
>
> `mergeConfig` принимает конфигурацию только в форме объекта. Если у вас конфигурация в форме колбэка, вам следует вызвать её перед передачей в `mergeConfig`.
>
> Вы можете использовать вспомогательную функцию `defineConfig`, чтобы объединить конфигурацию в форме колбэка с другой конфигурацией:
>
> ```ts
> import {
>   defineConfig,
>   mergeConfig,
>   type UserConfigFnObject,
>   type UserConfig,
> } from 'vite'
> declare const configAsCallback: UserConfigFnObject
> declare const configAsObject: UserConfig
>
> export default defineConfig((configEnv) =>
>   mergeConfig(configAsCallback(configEnv), configAsObject),
> )
> ```

## `searchForWorkspaceRoot`

**Сигнатура типа:**

```ts
function searchForWorkspaceRoot(
  current: string,
  root = searchForPackageRoot(current),
): string
```

**См. также:** [server.fs.allow](https://vite.dev/config/server-options#server-fs-allow)

Ищет корень потенциального рабочего пространства (workspace), если он удовлетворяет следующим условиям, иначе возвращается к `root`:

- содержит поле `workspaces` в `package.json`
- содержит один из следующих файлов
  - `lerna.json`
  - `pnpm-workspace.yaml`

## `loadEnv`

**Сигнатура типа:**

```ts
function loadEnv(
  mode: string,
  envDir: string,
  prefixes: string | string[] = 'VITE_',
): Record<string, string>
```

**См. также:** [Файлы `.env`](https://vite.dev/guide/env-and-mode#env-files)

Загружает файлы `.env` внутри `envDir` и объединяет их с соответствующими переменными, уже присутствующими в `process.env`. По умолчанию загружаются только переменные окружения с префиксом `VITE_`, если только не изменён `prefixes`.

## `normalizePath`

**Сигнатура типа:**

```ts
function normalizePath(id: string): string
```

**См. также:** [Нормализация путей](https://vite.dev/guide/api-plugin#path-normalization)

Нормализует путь для обеспечения совместимости между плагинами Vite.

## `transformWithOxc`

**Сигнатура типа:**

```ts
async function transformWithOxc(
  code: string,
  filename: string,
  options?: OxcTransformOptions,
  inMap?: object,
): Promise<Omit<OxcTransformResult, 'errors'> & { warnings: string[] }>
```

Преобразует JavaScript или TypeScript с помощью [Oxc Transformer](https://oxc.rs/docs/guide/usage/transformer). Полезно для плагинов, которые предпочитают соответствовать внутреннему преобразованию Vite через Oxc Transformer.

## `transformWithEsbuild`

**Сигнатура типа:**

```ts
async function transformWithEsbuild(
  code: string,
  filename: string,
  options?: EsbuildTransformOptions,
  inMap?: object,
): Promise<ESBuildTransformResult>
```

**Устарело:** используйте `transformWithOxc` вместо этого.

Преобразует JavaScript или TypeScript с помощью esbuild. Полезно для плагинов, которые предпочитают соответствовать внутреннему преобразованию Vite через esbuild.

## `loadConfigFromFile`

**Сигнатура типа:**

```ts
async function loadConfigFromFile(
  configEnv: ConfigEnv,
  configFile?: string,
  configRoot: string = process.cwd(),
  logLevel?: LogLevel,
  customLogger?: Logger,
): Promise<{
  path: string
  config: UserConfig
  dependencies: string[]
} | null>
```

Загружает файл конфигурации Vite вручную с помощью Rolldown.

## `preprocessCSS`

- **Экспериментально:** [Оставить отзыв](https://github.com/vitejs/vite/discussions/13815)

**Сигнатура типа:**

```ts
async function preprocessCSS(
  code: string,
  filename: string,
  config: ResolvedConfig,
): Promise<PreprocessCSSResult>

interface PreprocessCSSResult {
  code: string
  map?: SourceMapInput
  modules?: Record<string, string>
  deps?: Set<string>
}
```

Выполняет предварительную обработку файлов `.css`, `.scss`, `.sass`, `.less`, `.styl` и `.stylus` в обычный CSS, чтобы его можно было использовать в браузерах или обрабатывать другими инструментами. Как и во [встроенной поддержке предобработки CSS](https://vite.dev/guide/features#css-pre-processors), соответствующий препроцессор должен быть установлен, если он используется.

Используемый препроцессор определяется по расширению `filename`. Если `filename` заканчивается на `.module.{ext}`, он распознаётся как [CSS-модуль](https://github.com/css-modules/css-modules), и возвращаемый результат будет включать объект `modules`, сопоставляющий исходные имена классов с преобразованными.

Обратите внимание, что предобработка не будет разрешать URL в `url()` или `image-set()`.

## `version`

**Тип:** `string`

Текущая версия Vite в виде строки (например, `"8.0.0"`).

## `rolldownVersion`

**Тип:** `string`

Версия Rolldown, используемая Vite, в виде строки (например, `"1.0.0"`). Реэкспорт [`VERSION`](https://rolldown.rs/reference/Variable.VERSION) из `rolldown`.

## `esbuildVersion`

**Тип:** `string`

Сохранено только для обратной совместимости.

## `rollupVersion`

**Тип:** `string`

Сохранено только для обратной совместимости.

---

> _Перевод официальной документации Vite «JavaScript API». Источник: https://vite.dev/guide/api-javascript. Оригинал распространяется по лицензии [MIT](https://github.com/vitejs/vite/blob/main/LICENSE)._
